/** @type {import('next').NextConfig} */
const nextConfig = {
    compiler: {
        reactStrictMode: true,
        styledComponents: true
    },
    output: "standalone",
}

module.exports = nextConfig
